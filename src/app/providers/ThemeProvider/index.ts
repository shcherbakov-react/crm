import ThemeProvider from './ui/ThemeProvider';
// import { useTheme } from './lib/useTheme';
// import { Theme } from './lib/ThemeContext';
import ThemeProviderComponent from './ui/ThemeProvider';

export {
    ThemeProviderComponent,
    // useTheme,
    // Theme,
};

export class Theme {
    static LIGHT: any;
    static DARK: any;
}
